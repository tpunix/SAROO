
cdgplay
-------

简单的cdg文件播放器。将sub文件或cdg文件拖放上去即可播放。
默认是无延时全速播放。在命令行使用，可以附加一个速度参数，将以指定的倍率播放。

cdgfix
------

修复sub或cdg文件中的错误。大多数cdg文件从CD读出后，并没有进行纠错。这将导致播放时出现色块与花屏。
将文件拖放上去即可进行纠错并自动保存。会显示纠错的块的数量与不可修复的块的数量。
